{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug App",
            "program": "${workspaceFolder}/build/source/simreple",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build All"
        }
    ]
}